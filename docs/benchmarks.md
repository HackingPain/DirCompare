# Benchmarks

Run the benchmark suite:

```bash
python benchmarks/benchmark.py
```

See `benchmarks/README.md` for details on what is measured.

Results vary by machine. Run locally for accurate numbers.
